akka {
  loglevel = "ERROR"
  log-dead-letters = 0
  log-dead-letters-during-shutdown = off
}

registry {
  url = "jdbc:sqlite:registry.db"
  driver = org.sqlite.JDBC
  connectionPool = disabled
  keepAliveConnection = true
}